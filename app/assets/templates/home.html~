<ul class="unstyled">
<li ng-repeat="post in posts">
	
	<h3 class="title">{{post.title}}</h3>
	<div class="row">
		<img src="{{post.image}}" height="150px" width="150px"/>
		<p>{{post.body}}</p>
	</div>
</li>

</ul>
