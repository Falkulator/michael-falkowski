<ul class="nav nav-tabs nav-stacked">
<li ng-repeat="post in posts">
	<div class="row-fluid span10 thumbnail">
		<a href="#/posts/{{post.id}}"><h3 class="title">{{post.title}}</h3></a>
		<img class="main" ng-src="{{post.image}}" height="150px" width="150px"/>
		<p class="main">{{post.body}}</p>
	</div>
</li>

</ul>
